{
  "version": "0.6.0",
  "download_url": "https://raw.githubusercontent.com/strummer95/bt-dtf/main/bt-dtf-0.6.0.zip",
  "homepage": "https://boomerts.com",
  "tested": "6.7",
  "changelog": "<h4>0.6.0</h4><ul><li>The completed-order email now matches how the customer is actually getting their order. A pickup order gets \"Your order is ready for pickup!\" and a subject to match, plus a callout above the order table with the shop address and phone. A shipped order gets \"Your order is on its way!\"</li><li>The stock wording only ever described shipping, so pickup customers were told their order was heading their way while it sat on the counter.</li><li>Pickup is detected from the shipping method on the order, by method ID or by a title containing \"pickup\", which covers a pickup option set up as a zero-cost flat rate.</li><li>An order with no shipping line at all keeps the stock wording rather than being guessed at.</li><li>The address comes from WooCommerce store settings, so there is no second copy to go stale.</li></ul><h4>0.5.2</h4><ul><li>The View cart link from 0.5.1 is now a cart icon in the top right of the builder, sitting in the preview header beside the zoom controls.</li></ul><h4>0.5.1</h4><ul><li>Added a <strong>View cart</strong> link under the Add to Cart button. The builder previously had no way back to the cart, and since Add to Cart is disabled on an empty canvas, anyone who added a sheet and then returned to the builder was stranded.</li></ul><h4>0.5.0</h4><ul><li>New <strong>Max name width</strong> setting in Names &amp; Numbers. Pieces were sized by height alone, so a long name at 2.5 inches tall could come out wider than the sheet. A name over the cap is now scaled down proportionally until it fits, so the letterforms keep their shape and the piece simply ends up shorter than the requested height.</li><li>Defaults to 11 inches. Set it to 0 for the old behavior of no limit.</li><li>The cap is also on each batch card, so you can change it after generating and the whole batch re-renders.</li><li>Numbers are not capped. They are meant to be large, and a shared cap would have quietly shrunk a two-digit number well below its requested height.</li><li>Sheets saved before this release reopen with no cap, exactly as they were left.</li></ul><h4>0.4.7</h4><ul><li>Fill roster is now a solid full-width button in the panel's blue rather than a small dashed outline, so it reads as the thing to click after pasting. Cancel sits beside it as a plain text link.</li></ul><h4>0.4.6</h4><ul><li>The Paste a list format examples now live in the paste box itself as placeholder text, and the separate example row below it is gone.</li></ul><h4>0.4.5</h4><ul><li>The Paste a list hint is now two short examples side by side instead of a paragraph of instructions.</li></ul><h4>0.4.4</h4><ul><li>The sheet preview now uses the standard transparency checkerboard instead of flat white. White and light artwork was invisible against a white sheet, so a roster of white names read as empty boxes.</li><li>The one-inch dot grid was darkened so it still reads against the checker.</li><li>Preview only. The printed sheet and the exported PNG are unchanged and still have a fully transparent background.</li></ul><h4>0.4.3</h4><ul><li><strong>Paste a list</strong> is now staff only. It renders for users with the manage_woocommerce capability and is absent from the page for everyone else, so customers see the roster exactly as they did before 0.4.2.</li><li>Same capability the rest of the plugin already uses for shop staff, so no new role or setting to manage.</li></ul><h4>0.4.2</h4><ul><li>Adds <strong>Paste a list</strong> to the Names &amp; Numbers roster. Paste a whole team in one go instead of typing each row.</li><li>One entry per line. The number can be separated from the name by a tab, comma or plain space, so a column pasted straight out of a spreadsheet or an order export works as-is.</li><li>A line with only digits becomes a number with no name, and a line with no digits becomes a name with no number, which covers the odd jersey that only wants one of the two.</li><li>Filling the roster replaces the rows currently in it, and the status line reports how many rows loaded.</li></ul><h4>0.4.1</h4><ul><li>Fixes the W and H boxes on a design card only accepting one character. Typing 12 committed the 1 and threw the field away before the 2 landed.</li><li>Cause: both boxes committed on every keystroke, which clamped the partial number and rebuilt the card list, destroying the input being typed into. They now commit on blur or Enter, the same way the copies box already did.</li><li>Clicking into either box now selects what is there, so you can type straight over it, and Enter commits without touching the mouse.</li></ul><h4>0.4.0</h4><ul><li>Adds the Frontend module &mdash; the [gang_sheet_builder] UI itself. All four DTF Studio snippets now live in the plugin.</li><li>The builder code is unchanged: same DOM ids and classes, same embedded fonts, same nesting and pricing logic, same AJAX actions and nonce. Only the PHP function name changed, plus the dormancy wrapper.</li><li>Verified before release: PHP lints clean, the JavaScript parses, and all 76 builder functions are present exactly once with no unresolved internal calls.</li><li>Like the other modules it stands down while its snippet is active, so the shortcode can never register twice.</li></ul><h4>0.3.3</h4><ul><li>Moved the sidebar menu to position 58, where BT Catalog, BT Portal and BT Quote already sit, so all four BT plugins group together instead of BT Transfers floating above them.</li><li>The first submenu entry now reads <em>Sheet Settings</em> rather than repeating the plugin name.</li></ul><h4>0.3.2</h4><ul><li>Adds the <strong>Status &amp; Updates</strong> page under BT Transfers, matching BT Portal, BT Catalog and BT Quote &mdash; installed vs latest version, and a <em>Check for updates now</em> button that clears the caches and links straight to the Plugins page.</li><li>The page also shows which modules are running from the plugin and which are still standing by behind an active WPCode snippet.</li></ul><h4>0.3.1</h4><ul><li>Renamed to <strong>BT Transfers</strong> &mdash; sidebar menu, settings page heading and plugin name &mdash; so it sits with BT Catalog, BT Portal and BT Quote in the admin menu.</li><li>The menu slug is unchanged (btgsb-settings), so any bookmark to the settings page still works, and every option, table and meta key is untouched.</li><li>The dormancy notice now only appears on the Plugins screen instead of every admin page.</li></ul><h4>0.3.0</h4><ul><li>Adds the Shipping and Save &amp; Resume modules, so three of the four DTF Studio snippets now live in the plugin.</li><li>Each module stands down independently while its own snippet is active, so you can deactivate them one at a time and check between each &mdash; no all-or-nothing cutover.</li><li>Shipping keeps the method ID btgsb_shipping, so your configured zone rates carry over. Save &amp; Resume keeps the wp_btgsb_saves table, its AJAX actions and the nightly cron hook, so existing resume links still work.</li><li>The Frontend snippet (the builder UI) is unchanged and still runs from WPCode. It talks to the plugin through the same AJAX actions and nonce, so it keeps working either way.</li></ul><h4>0.2.0</h4><ul><li>Fixes the fatal in 0.1.0. That build kept the snippet's btgsb_* function names, so activating it while the DTF Studio snippet was still on caused a PHP redeclare and took the site down. Every function is now btdtf_ and every constant BTDTF_ &mdash; zero names in common, so a redeclare cannot happen in any activation order.</li><li>Nothing registers at load. Hooks wait for plugins_loaded priority 999, after WPCode has evaluated its snippets, and the plugin stands fully dormant (no menu, no AJAX, no order columns, no duplicate meta) if the old snippet is still active &mdash; with a notice explaining why.</li><li>Option names, order meta keys, AJAX actions, the nonce, page slugs and CSS classes are unchanged, so existing orders, saved settings and the DTF Studio Frontend snippet keep working untouched.</li><li>Same coexistence model as BT Portal: activate the plugin, verify it, then deactivate the snippet whenever you like.</li></ul><h4>0.1.0</h4><ul><li>Initial plugin release &mdash; the DTF Studio &mdash; Backend snippet moved into a real plugin with dashboard auto-updates, so fixes arrive as an Update button instead of a paste.</li><li>Every option name, AJAX action, meta key and function name is unchanged, so existing orders, settings and pricing tiers carry over untouched. Nothing to migrate.</li><li>Conflict guard: if the old WPCode snippet is still active the plugin stays inert and shows a notice, rather than fatally redeclaring its functions and white-screening the site.</li><li>Includes the Awaiting Items tracking flag &mdash; a badge and filter on the Orders list that never changes an order's WooCommerce status, so flagged orders cannot drop out of the list.</li><li>The four custom order statuses (Art Approved, Printing, Ready for Pickup, Shipped) and their two customer emails remain removed.</li></ul>"
}